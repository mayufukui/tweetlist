<h1>id: <%= @tweet.id %> のツイート編集ページ</h1>

<%= forn_with(model: @tweet, local: true) do |f| %>
  <%= f.label :content, 'ツイート' %>
  <%= f.text_field :content %>
  
  <%= f.submit '投稿' %>
<% end %>

<%= link_to '一覧に戻る', tweets_path %>