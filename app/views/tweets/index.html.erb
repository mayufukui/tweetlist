<h1>ツイート一覧</h1>

<ul>
  <% @tweets.each do |tweet| %>
    <li><%= link_to tweet.id, tweet %> : <%= tweet.date %> &gt; <%= tweet.content %></li>
  <% end %>
</ul>
<%= paginate @tweets %>

<%= link_to '新規ツイートの投稿', new_tweet_path %>